import { makeStyles } from "@mui/styles";

export const useFlexiTitleStyles = makeStyles(() => ({
  title: {
    fontWeight: "600 !important",
    fontSize: "24px !important",
    lineHeight: "28px !important",
  },
}));
