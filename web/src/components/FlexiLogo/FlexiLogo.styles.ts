import { makeStyles } from "@mui/styles";

export const useFlexiLogoStyles = makeStyles(() => ({
  logo: {
    fontWeight: "600 !important",
    fontSize: "28px !important",
    lineHeight: "42px !important",
  },
}));
