import { makeStyles } from "@mui/styles";

export const useSidebarListStyles = makeStyles(() => ({
  list: { minHeight: "180px" },
  listItem: { paddingRight: "0 !important", paddingLeft: "0 !important" },
}));
