import { makeStyles } from "@mui/styles";

export const useConnectWalletStyles = makeStyles(() => ({
  connectWrapper: {
    position: "fixed",
    bottom: "50px",
  },
  connect: { width: "100% !important" },
}));
