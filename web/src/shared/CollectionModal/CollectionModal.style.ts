import { makeStyles } from "@mui/styles";

export const useCollectionModalStyles = makeStyles(() => ({
  card: {
    padding: "30px 60px !important",
  },
  title: { marginRight: "30px !important" },
}));
